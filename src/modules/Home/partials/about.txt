
import { ArrowRight, Shield, GitBranch, Code, Zap } from "lucide-react"; 

import React from "react";

// Core competencies focused on single word impact and minimalist description
const professionalCommitments = [
    { 
        icon: Code, 
        title: "LEADERSHIP", 
        detail: "Driving architectural vision and modern performance standards across development teams.", 
        color: "text-indigo-400", // Primary accent color
    },
    { 
        icon: Shield, 
        title: "RESILIENCE", 
        detail: "Implementing robust error handling and automated testing for guaranteed platform stability.", 
        color: "text-red-400", // Secondary accent color
    },
    { 
        icon: GitBranch, 
        title: "OWNERSHIP", 
        detail: "End-to-end management of the product lifecycle, from concept to production monitoring.", 
        color: "text-yellow-400", // Matches Hero's name tag accent
    },
];


const AboutSection = () => {
    return (
        <section 
            id="about" 
            // Enforce dark theme to match Hero Section
            className="py-32 md:py-40 bg-gray-950 text-white px-4 sm:px-6 lg:px-8"
        >
            <div className="container mx-auto max-w-7xl">
                
                {/* Header: Centered Subtitle */}
                <div className="text-center mb-20">
                    <p className="text-base font-semibold tracking-[0.3em] uppercase text-yellow-400">
                        FULL-STACK ARCHITECT
                    </p>
                </div>

                {/* Main Content Grid: Asymmetrical Split (70/30) on desktop */}
                <div className="grid grid-cols-1 lg:grid-cols-12 gap-16 lg:gap-10 items-start">
                    
                    {/* 1. Left Column (Narrative & CTA) - 7/12 columns on large screens */}
                    <div className="lg:col-span-7 space-y-8">
                        
                        {/* Main Headline */}
                        <h2 className="text-5xl md:text-6xl font-extrabold tracking-tighter leading-tight text-white">
                            Engineered for <span className="text-indigo-400">Scale.</span> Reliable.
                        </h2>
                        
                        {/* Narrative Summary */}
                        <p className="text-xl text-gray-300 leading-relaxed font-normal max-w-xl">
                   My name is Mohammad, and my expertise lies in solving complex technical challenges.** I architect and build high-availability solutions using modern stacks like **Next.js, React, and Node.js**. My goal is to ensure your applications deliver unparalleled **Performance, Maintainability, and Security
                        </p>

                        {/* Professional CTA Button - Matches Hero Section styling */}
                        <div className="pt-6">
                            <a href="#contact" 
                                className="inline-flex items-center justify-center text-lg font-bold uppercase 
                                            bg-indigo-600 hover:bg-indigo-700 text-white px-10 py-4 
                                            rounded-lg shadow-xl shadow-indigo-500/50 transition-all duration-300 transform hover:scale-[1.03] focus:ring-4 focus:ring-indigo-300">
                                Initiate Project Inquiry <ArrowRight className="ml-3 h-5 w-5"/>
                            </a>
                        </div>
                    </div>

                    {/* 2. Right Column (Manifesto List) - 5/12 columns on large screens */}
                    <div className="lg:col-span-5 bg-gray-900 p-8 rounded-xl border border-gray-800 shadow-xl">
                        
                        <h3 className="text-xl font-bold text-white uppercase tracking-wider mb-6 border-b border-indigo-700 pb-3">
                            Engineering Manifesto
                        </h3>

                        <div className="space-y-6">
                            {professionalCommitments.map((item, index) => {
                                const Icon = item.icon;
                                return (
                                    <div key={index} 
                                        className="flex items-start space-x-4 group transition-all duration-200 p-3 -m-3 rounded-lg hover:bg-gray-800/50">
                                        
                                        {/* Icon */}
                                        <div className="flex-shrink-0 pt-1">
                                            <Icon className={`h-6 w-6 ${item.color}`} />
                                        </div>
                                        
                                        {/* Content */}
                                        <div>
                                            <h4 className="text-lg font-extrabold text-white uppercase tracking-wider group-hover:text-indigo-400 transition-colors">
                                                {item.title}
                                            </h4>
                                            <p className="text-sm text-gray-400 leading-relaxed pt-0.5">
                                                {item.detail}
                                            </p>
                                        </div>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                </div>
            </div>
        </section>
    );
};

export default AboutSection



import { ArrowRight, Code, Shield, GitBranch, Terminal } from "lucide-react";

import Link from "next/link";

const AboutSectionV5 = () => {
    
    // Core competencies framed as a professional manifesto
    const professionalManifesto = [
        { 
            icon: Shield, 
            title: "System Resilience", 
            description: "Implementing robust error handling and automated testing (Jest, Cypress) to guarantee platform stability under peak traffic.", 
            accent: "text-indigo-400" 
        },
        { 
            icon: Code, 
            title: "Technical Leadership", 
            description: "Guiding teams through complex architectural decisions, code reviews, and adoption of modern, performant standards.", 
            accent: "text-yellow-400" 
        },
        { 
            icon: GitBranch, 
            title: "End-to-End Ownership", 
            description: "Managing the full product lifecycle, from initial concept wireframing to production deployment and monitoring (DevOps).", 
            accent: "text-red-400" 
        },
    ];

    return (
        // Base container: Sharp contrast, defined edges
        <section 
            id="about" 
            className="relative py-32 md:py-40  text-white overflow-hidden border-t-8 border-indigo-600"
        >
            {/* Background Texture: Very subtle, professional linear pattern */}
            <div className="absolute inset-0 z-0 opacity-[0.03] pointer-events-none" 
                 style={{ 
                    backgroundImage: 'linear-gradient(90deg, #334155 1px, transparent 0), linear-gradient(180deg, #334155 1px, transparent 0)', 
                    backgroundSize: '100px 100px',
                 }}
            ></div>

            <div className="container mx-auto max-w-7xl relative z-10 px-4">
                
                {/* Header Block with strong separation */}
                <div className="mb-16 pb-10 border-b border-gray-700">
                    <p className="text-lg font-black uppercase tracking-[0.3em] text-yellow-400/80">
                        The Full-Stack Architect
                    </p>
                    <h2 className="mt-4 text-6xl sm:text-7xl font-extrabold tracking-tighter">
                        My Value Is **Scale**. <span className="text-indigo-400">My Specialty Is Architecture.</span>
                    </h2>
                </div>

                {/* Main Content Grid: Balanced Text and Manifesto */}
                <div className="grid grid-cols-1 lg:grid-cols-2 gap-16 items-start">
                    
                    {/* Left Column: Core Narrative and Key Skills */}
                    <div className="space-y-8">
                        
                        {/* High-Impact Summary Quote */}
                        <blockquote className="text-2xl font-semibold italic text-white border-l-8 border-yellow-400 pl-6 py-4 bg-gray-800/80 rounded-r-lg shadow-2xl">
                            "I architect scalable platforms using **Next.js and Node.js**, transforming complex technical requirements into high-availability solutions for demanding environments."
                        </blockquote>

                        <p className="text-lg text-gray-400 leading-relaxed">
                            My expertise is dedicated to the core principles of software engineering: **Performance, Maintainability, and Security**. I manage the entire technology stack, ensuring seamless communication between client-side interfaces (React/Next.js) and robust backend services (Node.js/TypeScript).
                        </p>
                        
                        <p className="text-lg text-gray-400 leading-relaxed">
                            I focus on leveraging **cloud infrastructure (AWS/Azure)** and modern DevOps practices to deliver continuous value, minimize deployment risks, and maximize overall system efficiency.
                        </p>

                        {/* Professional CTA */}
       
                            <a className="mt-8 inline-flex items-center text-xl font-bold uppercase text-indigo-400 hover:text-yellow-400 transition-colors group">
                                Explore My Technical Projects 
                                <Terminal className="ml-3 h-6 w-6 group-hover:rotate-6 transition-transform"/>
                            </a>
                 
                    </div>

                    {/* Right Column: Professional Manifesto Cards */}
                    <div className="space-y-6">
                        <h3 className="text-3xl font-bold text-white mb-6 border-b pb-3 border-indigo-700">
                            Professional Commitments
                        </h3>
                        {professionalManifesto.map((item, index) => (
                            <ManifestoCard key={index} {...item} />
                        ))}
                    </div>
                </div>
            </div>
        </section>
    );
};

// Helper component for the professional manifesto cards
const ManifestoCard = ({ icon: Icon, title, description, accent }) => (
    <div className={`p-6 bg-gray-800 rounded-xl border border-gray-700 shadow-xl transition-all duration-300 hover:bg-gray-700/70 transform hover:translate-x-1`}>
        <div className="flex items-start space-x-4">
            <div className={`flex-shrink-0 p-3 rounded-full bg-indigo-600 shadow-lg ${accent}`}>
                <Icon className="h-6 w-6 text-white" />
            </div>
            <div>
                <h4 className="text-xl font-extrabold text-white mb-1 uppercase tracking-wider">{title}</h4>
                <p className="text-gray-400 text-base">{description}</p>
            </div>
        </div>
    </div>
);


export default AboutSectionV5;

// const ManifestoCard = ({ icon: Icon, title, description, accent }) => (
//     <div className={`p-6 bg-gray-800 rounded-xl border border-gray-700 shadow-xl transition-all duration-300 hover:bg-gray-700/70 transform hover:translate-x-1`}>
//         <div className="flex items-start space-x-4">
//             <div className={`flex-shrink-0 p-3 rounded-full bg-indigo-600 shadow-lg ${accent}`}>
//                 <Icon className="h-6 w-6 text-white" />
//             </div>
//             <div>
//                 <h4 className="text-xl font-extrabold text-white mb-1 uppercase tracking-wider">{title}</h4>
//                 <p className="text-gray-400 text-base">{description}</p>
//             </div>
//         </div>
//     </div>

{/* <div className="mt-20 pt-10 border-t border-gray-700">
            <h3 className="text-center text-3xl font-bold mb-10 text-white">
                My Core Differentiators
            </h3>
            <div className="grid grid-cols-1 md:grid-cols-3 gap-8">
                {differentiators.map((item, index) => (
                    <DifferentiatorCard key={index} {...item} />
                ))}
            </div>
        </div> */}

// const DifferentiatorCard = ({ icon: Icon, title, detail, color }) => (
//     <div className={`p-6 bg-gray-800 rounded-xl shadow-2xl border-t-4 border-indigo-600 transition-all duration-300 hover:bg-gray-700/50 hover:shadow-indigo-700/50 transform hover:-translate-y-1`}>
//         <div className={`p-3 rounded-full bg-indigo-600/20 inline-block mb-4 ${color}`}>
//             <Icon className="h-8 w-8" />
//         </div>
//         <h4 className="text-2xl font-extrabold text-white mb-2">{title}</h4>
//         <p className="text-gray-400">{detail}</p>
//     </div>
// );

// const FeatureItem = ({ icon: Icon, title, description }) => (
//     <div className="flex items-start space-x-4">
//         <div className="flex-shrink-0 p-3 rounded-full bg-indigo-600 text-white shadow-lg">
//             <Icon className="h-6 w-6" />
//         </div>
//         <div>
//             <h3 className="text-xl font-bold text-white">{title}</h3>
//             <p className="mt-1 text-gray-400">{description}</p>
//         </div>
//     </div>

{/* <div className="space-y-6">
                <FeatureItem 
                    icon={Zap} 
                    title="Performance First" 
                    description="Specializing in Next.js and server-side rendering (SSR) to ensure lightning-fast load times and superior SEO scores."
                />
                <FeatureItem 
                    icon={Server} 
                    title="Enterprise Scalability" 
                    description="Expertise in Node.js and robust cloud architectures (AWS/Azure) for backend systems designed to handle millions of users."
                />
                <FeatureItem 
                    icon={Layers} 
                    title="Clean Code Advocacy" 
                    description="Committed to writing elegant, tested, and well-documented TypeScript code that minimizes technical debt for future growth."
                />
            </div> */}

// import { ArrowRight, Briefcase, Zap } from "lucide-react";

// // (Keeping these defined locally for self-contained execution)

// // 1. Integrated Button Component (based on the original usage)
// const CustomButton = ({ children, className, ...props }) => (
//   <button
//     className={`flex items-center justify-center px-8 py-3 rounded-xl shadow-lg transition duration-300 ease-in-out ${className}`}
//     {...props}
//   >
//     {children}
//   </button>
// );

// // 2. Integrated Card Component (GlassCard equivalent, preserving styles)
// const CustomGlassCard = ({ children, className }) => (
//   <div className={`rounded-xl ${className}`}>
//     {children}
//   </div>
// );

// // 3. Integrated Link Component (Replacing next/link with simple anchor)
// const CustomLink = ({ href, children }) => (
//   <a href={href} className="flex items-center">
//     {children}
//   </a>
// );

// // --- Main About Component (Asymmetrical Split Design) ---
// const About = () => {
//   // --- Style Constants for Consistency ---
//   const ACCENT_COLOR = "text-yellow-400";
//   const ACCENT_BG = "bg-yellow-400";
//   const PRIMARY_BUTTON = "bg-yellow-400 hover:bg-yellow-300 text-indigo-950";
//   const BUTTON_SHADOW = "drop-shadow-[0_0_18px_rgba(250,204,21,1)]"; 
//   const DEEP_BACKGROUND = "bg-indigo-950";
//   const CARD_BACKGROUND = "bg-indigo-900"; 
//   const MILD_TEXT = "text-indigo-200";
//   const BOLD_TEXT = "text-white";

//   // Data for the stat cards
//   const stats = [
//     { number: "41+", label: "Projects Launched", icon: Briefcase },
//     { number: "98%", label: "Code Quality Score", icon: Zap },
//   ];

//   return (
//     // Section: Full-width, centered content, and dark background
//     <section id="about" className={`py-28 px-6 relative  overflow-hidden`}>
      
//       {/* Dynamic Background Element */}
//       <div 
//         className="absolute top-1/2 left-1/2 w-4/5 h-4/5 
//                    bg-radial-gradient-indigo opacity-30 pointer-events-none 
//                    mix-blend-lighten transform -translate-x-1/2 -translate-y-1/2 rotate-3"
//         style={{
//           background: 'radial-gradient(circle at center, rgba(67, 56, 202, 0.4), rgba(250, 204, 21, 0.1), transparent 70%)',
//           zIndex: 0
//         }}
//       />

//       <div className="container mx-auto max-w-7xl relative z-10">
        
//         {/* Asymmetrical Grid: 2/3 for Narrative, 1/3 for Stats on desktop */}
//         <div className="grid lg:grid-cols-3 gap-16 lg:gap-8 items-start">
          
//           {/* LEFT COLUMN: Narrative and CTA (2/3 width) */}
//           <div className="lg:col-span-2 space-y-8 text-left animate-fade-in">
            
//             {/* Pre-Header */}
//             <p className={`${ACCENT_COLOR} text-md font-extrabold tracking-[0.3em] uppercase`}>
//                 THE DEVELOPMENT PHILOSOPHY
//             </p>
            
//             {/* Main Header: Massive, high-impact typography */}
//             <h2 className="text-5xl sm:text-6xl md:text-7xl font-extrabold leading-tight text-white">
//                 Crafting Digital <span className={ACCENT_COLOR}>Identity</span>.
//                 <br /> Building Your <span className={ACCENT_COLOR}>Success.</span>
//             </h2>

//             {/* Main Paragraph */}
//             <p className="text-xl md:text-2xl leading-relaxed text-indigo-100 max-w-2xl">
//                 I transform complex ideas into simple, high-performance web applications. Focused on **React and modern frontend stacks**, my work is defined by an obsession with clean code, intuitive UX, and delivering measurable business value.
//             </p>

//             {/* Key Skills/Tags */}
//             <div className="flex flex-wrap gap-4 pt-4">
//               <div className={`px-5 py-2.5 ${CARD_BACKGROUND} rounded-full border border-indigo-700 font-semibold hover:border-yellow-400 transition-colors duration-300`}>
//                 <p className="text-white">React & Next.js</p>
//               </div>
//               <div className={`px-5 py-2.5 ${CARD_BACKGROUND} rounded-full border border-indigo-700 font-semibold hover:border-yellow-400 transition-colors duration-300`}>
//                 <p className="text-white">
//                   Creative UI/UX
//                 </p>
//               </div>
//               <div className={`px-5 py-2.5 ${CARD_BACKGROUND} rounded-full border border-indigo-700 font-semibold hover:border-yellow-400 transition-colors duration-300`}>
//                 <p className="text-white">
//                   Typescript First
//                 </p>
//               </div>
//             </div>

//             {/* CTA Button */}
//             <CustomLink href="#contact">
//               <CustomButton 
//                 className={`group font-bold text-xl transition-all duration-300 
//                            ${PRIMARY_BUTTON} ${BUTTON_SHADOW} mt-8`}
//               >
//                 Start a Conversation
//                 <ArrowRight className="ml-3 w-6 h-6 group-hover:translate-x-1 transition-transform" />
//               </CustomButton>
//             </CustomLink>
//           </div>

//           {/* RIGHT COLUMN: Profile and Stats (1/3 width, visually striking) */}
//           <div className={`lg:col-span-1 space-y-8 pt-12 lg:pt-0 border-t-2 lg:border-t-0 lg:border-l-2 border-indigo-700/50 lg:pl-8 lg:mt-6 animate-fade-in`} style={{ animationDelay: "0.3s" }}>
            
//             {/* 1. Profile/Avatar Section (Simplified and integrated) */}
//             <div className="text-center lg:text-left">
//                 <div className={`inline-flex w-28 h-28 rounded-full 
//                                 ${ACCENT_BG} items-center justify-center 
//                                 text-indigo-950 text-5xl font-black 
//                                 shadow-2xl ${BUTTON_SHADOW} ring-4 ring-yellow-400/50 mb-4`}>
//                     JD
//                 </div>
//                 <h3 className="text-2xl font-bold text-white">John Doe, Developer</h3>
//                 <p className={`${MILD_TEXT} text-sm italic`}>
//                     Based in San Francisco, CA
//                 </p>
//             </div>


//             {/* 2. Stat Cards (Smaller, Vertical Layout) */}
//             <div className="space-y-6 pt-4">
//                 {stats.map((stat, index) => (
//                     <CustomGlassCard 
//                         key={index}
//                         className={`p-6 rounded-xl transition-all duration-300 
//                                    ${CARD_BACKGROUND} border-2 border-indigo-700 
//                                    hover:border-yellow-400 hover:scale-[1.05] cursor-pointer
//                                    drop-shadow-[0_10px_20px_rgba(55,48,163,0.7)]`}
//                     >
//                         <div className="flex items-center gap-4">
//                             <stat.icon className={`w-6 h-6 ${ACCENT_COLOR}`} />
//                             <div>
//                                 <p className={`text-4xl font-extrabold ${ACCENT_COLOR} leading-none`}>
//                                     {stat.number}
//                                 </p>
//                                 <p className="text-sm font-medium text-white mt-1">
//                                     {stat.label}
//                                 </p>
//                             </div>
//                         </div>
//                     </CustomGlassCard>
//                 ))}
//             </div>
            
//           </div>
//         </div>
//       </div>
//     </section>
//   );
// };

// export default About;



 // ManifestoCard.tsx (Reusable Component - Unchanged for consistency)

import { LucideIcon } from "lucide-react";
import React from "react";
import { Code, Shield, GitBranch, Terminal } from "lucide-react";
import Link from "next/link";

interface ManifestoCardProps {
    icon: LucideIcon;
    title: string;
    description: string;
}

const ManifestoCard: React.FC<ManifestoCardProps> = ({ icon: Icon, title, description }) => (
    <div className={`
        // Base Styling: Dark, sharp box, full height
        p-6 h-full bg-gray-900 rounded-lg border-2 border-gray-800 shadow-2xl 
        // Hover Effect: Aggressive glow using Yellow-400
        transition-all duration-300 hover:border-yellow-400 transform hover:scale-[1.02] hover:shadow-yellow-900/50
    `}>
        <div className="flex flex-col space-y-4">
            {/* Icon: Indigo accent for maximum contrast */}
            <div className={`flex-shrink-0 p-3 rounded-md bg-yellow-400 shadow-lg w-fit`}>
                <Icon className="h-7 w-7 text-indigo-900" /> 
            </div>
            
            <h4 className="text-2xl font-extrabold text-white uppercase tracking-widest">{title}</h4>
            <p className="text-gray-400 text-base leading-relaxed">{description}</p>
        </div>
    </div>
);

// --------------------------------------------------------------------------------------------------

// AboutSectionV8.tsx (Main Component - New Defiant Layout)


// import ManifestoCard from './ManifestoCard'; // Ensure this path is correct

const AboutSectionV8 = () => {
    
    // Core Commitments: Used for the three-column layout
    const coreCommitments = [
        { 
            icon: Shield, 
            title: "SCALE & PERFORMANCE", 
            description: "Architecting high-availability systems with Next.js and Node.js to handle enterprise-level loads. Focus on guaranteed sub-200ms API response times.", 
        },
        { 
            icon: Code, 
            title: "CODE STANDARDS", 
            description: "Enforcing clean architecture, TypeScript rigidity, and TDD (Jest/Cypress) to deliver maintainable, failure-resistant codebases.", 
        },
        { 
            icon: GitBranch, 
            title: "DEVOPS CHAIN", 
            description: "Full product ownership, managing CI/CD pipelines and monitoring on AWS/Azure, treating infrastructure as code for minimal deployment risk.", 
        },
    ];

    return (
        <section 
            id="about" 
            className="relative  py-24 md:py-36 text-white overflow-hidden"
        >
            {/* Massive Background Text Overlay: Changed to 'ARCHITECTURE' */}
            <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 text-center whitespace-nowrap opacity-[0.05] pointer-events-none z-0">
                <p className="text-[10rem] sm:text-[16rem] md:text-[24rem] font-black text-indigo-900 uppercase tracking-tighter leading-none transform -rotate-3">
                    ARCHITECT
                </p>
            </div>

            <div className="container mx-auto max-w-7xl relative z-10 px-4">
                
                {/* 1. HEADER & HIGH-IMPACT QUOTE (Full Width) */}
                <div className="text-center lg:text-left">
                    <p className="text-xl font-black uppercase tracking-[0.3em] text-yellow-400">
                        The Full-Stack Specialist
                    </p>
                    {/* Defiant Heading: Split and bolded aggressively */}
                    <h2 className="mt-4 text-6xl sm:text-7xl md:text-8xl font-black tracking-tighter leading-tight">
                        I DON'T JUST CODE. 
                        <br/> I <span className="text-indigo-400 border-b-4 border-yellow-400 pb-2">DESIGN SYSTEMS.</span>
                    </h2>
                </div>
                
                <div className="mt-12 max-w-4xl space-y-4 lg:ml-auto">
                    {/* Quote Block: Placed to the side for a deconstructed look */}
                    <blockquote className="text-2xl font-bold italic text-white border-l-8 border-yellow-400 pl-6 py-4 bg-gray-900 rounded-r-xl shadow-2xl">
                        "My focus is translating requirements into **high-availability solutions**, managing the full stack from **Next.js** interfaces to **Node.js** services."
                    </blockquote>
                </div>
                
                {/* Narrative Text */}
                <div className="max-w-xl mt-8 space-y-4 lg:ml-auto">
                    <p className="text-xl text-gray-300 leading-relaxed font-medium">
                        My expertise is dedicated to the core principles of software engineering: **Performance, Maintainability, and Security**. I ensure seamless communication between client-side interfaces and robust backend services, focusing on efficiency and risk minimization.
                    </p>
                </div>


               
                </div>

        </section>
    );
};

export default AboutSectionV8;



'use client';

import { ArrowRight, Shield, GitBranch, Code, Zap } from "lucide-react"; 
import React from "react"; 
import { AbsolutePosition, PositionStyle } from "../config/types";


// Central point for SVG curves and icon positioning (normalized to the 900x750 viewBox)
const VIEWBOX_W = 900;
const VIEWBOX_H = 750;
const CORE_X = VIEWBOX_W / 2; // 450
const CORE_Y = VIEWBOX_H * 0.62; // 465

// Utility function to convert percentage-based positioning to absolute pixel coordinates
const getAbsPosition = (style: PositionStyle): AbsolutePosition=> {
    const topPct = parseFloat(style.top);
    const leftPct = parseFloat(style.left);
    const absX = (leftPct / 100) * VIEWBOX_W;
    const absY = (topPct / 100) * VIEWBOX_H;
    
    return {
        absX,
        absY,
        relX: absX - CORE_X, 
        relY: absY - CORE_Y,
    };
};

// Placeholder URLs for the logos
const LOGO_MAP: { [key: string]: string } = {
    Figma: 'https://placehold.co/100x100/f24e1e/ffffff?text=Fg', 
    React: 'https://placehold.co/100x100/61DAFB/000000?text=R',
    CSharp: 'https://placehold.co/100x100/9B418E/ffffff?text=C#', 
    Node: 'https://placehold.co/100x100/339933/ffffff?text=N',
    JS: 'https://placehold.co/100x100/F7DF1E/000000?text=JS', 
    TS: 'https://placehold.co/100x100/3178C6/ffffff?text=TS',
    Xd: 'https://placehold.co/100x100/FF61F6/000000?text=Xd', 
    Next: 'https://placehold.co/100x100/000000/ffffff?text=NX',
    AI: 'https://placehold.co/100x100/FF9A00/000000?text=AI', 
    Express: 'https://placehold.co/100x100/000000/ffffff?text=EX',
    MongoDB: 'https://placehold.co/100x100/47A248/ffffff?text=MG',
    LinkedIn: 'https://placehold.co/100x100/0A66C2/ffffff?text=in',
    Google: 'https://placehold.co/100x100/4285F4/ffffff?text=G', 
    Behance: 'https://placehold.co/100x100/053EFF/ffffff?text=B',
};

const SkillIcon: React.FC<SkillIconProps> = ({ name, size = 'w-10 h-10', style }) => {
    const baseClasses = `absolute ${size} rounded-full flex items-center justify-center transition-all duration-300 transform -translate-x-1/2 -translate-y-1/2 z-20`;
    const imageSrc = LOGO_MAP[name] || LOGO_MAP.Figma;

    // Outer glow for orbiting icons only (checked via the presence of relX in style)
    const glowClasses = style.relX !== undefined ? 'shadow-sm shadow-purple-500/80' : 'shadow-lg shadow-black/30';

    const handleError = (e: React.SyntheticEvent<HTMLImageElement, Event>) => {
        const target = e.currentTarget;
        target.onerror = null; 
        target.style.backgroundColor = '#6d28d9'; 
        target.alt = name; 
        target.src = LOGO_MAP[name]; 
    };

    return (
        <div className={`${baseClasses} ${glowClasses}`} style={style}>
            <img 
                src={imageSrc} 
                alt={name} 
                className="w-full h-full object-cover rounded-full" 
                onError={handleError}
            />
        </div>
    );
};

const OrbitingGlyph: React.FC<OrbitingGlyphProps> = ({ name, style }) => {
    const glyphs: { [key: string]: JSX.Element | string } = {
        'AI': <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><circle cx="12" cy="12" r="10"></circle><path d="M16 16l-4-4-4 4"></path></svg>,
        'Code': <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><polyline points="16 18 22 12 16 6"></polyline><polyline points="8 6 2 12 8 18"></polyline></svg>,
        'Gear': <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><circle cx="12" cy="12" r="3"></circle><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1 0 2.83 2 2 0 0 1-2.83 0l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-2 2 2 2 0 0 1-2-2v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83 0 2 2 0 0 1 0-2.83l.06-.06a1.65 1.65 0 0 0 .33-1.82 1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1-2-2 2 2 0 0 1 2-2h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 0-2.83 2 2 0 0 1 2.83 0l.06.06a1.65 1.65 0 0 0 1.82.33H9a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 2-2 2 2 0 0 1 2 2v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 0 2 2 0 0 1 0 2.83l-.06.06a1.65 1.65 0 0 0-.33 1.82V9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 2 2 2 2 0 0 1-2 2h-.09a1.65 1.65 0 0 0-1.51 1z"></path></svg>,
        'JS': <span className="font-bold text-xs">JS</span>,
        'Box': <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M21 8a2 2 0 0 0-2-2H5a2 2 0 0 0-2 2v10a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2z"></path><path d="M12 6V3"></path><path d="M12 18V21"></path></svg>,
    };
    
    const baseClasses = `absolute w-5 h-5 flex items-center justify-center transform -translate-x-1/2 -translate-y-1/2 z-20 
                         text-purple-400 hover:text-indigo-400 transition-colors duration-300`;

    return (
        <div className={baseClasses} style={style}>
            {glyphs[name] || <span className="font-bold text-xs">{name}</span>}
        </div>
    );
};

const SkillOrbitV2: React.FC<SkillOrbitV2Props> = () => {
    // 1. TOP FANNING SKILLS (Start of the path)
    const topSkills: { name: string; style: PositionStyle }[] = [
        { name: 'Figma', style: { top: '15%', left: '20%' } },
        { name: 'React', style: { top: '15%', left: '30%' } },
        { name: 'CSharp', style: { top: '15%', left: '40%' } },
        { name: 'Node', style: { top: '15%', left: '60%' } },
        { name: 'JS', style: { top: '15%', left: '70%' } },
        { name: 'TS', style: { top: '15%', left: '80%' } },
        
        { name: 'Xd', style: { top: '25%', left: '25%' } },
        { name: 'Next', style: { top: '25%', left: '35%' } },
        { name: 'AI', style: { top: '25%', left: '65%' } },
        { name: 'Express', style: { top: '25%', left: '75%' } },
        { name: 'MongoDB', style: { top: '25%', left: '85%' } },
    ];

    // 2. ORBITING ICONS (Icons that need to move)
    const orbitingIcons: { component: React.FC<SkillIconProps | OrbitingGlyphProps>; name: string; size?: string; style: PositionStyle }[] = [
        // Large Logos 
        { component: SkillIcon, name: 'LinkedIn', size: 'w-7 h-7', style: { top: '40%', left: '10%' } }, 
        { component: SkillIcon, name: 'Google', size: 'w-7 h-7', style: { top: '65%', left: '5%' } }, 
        { component: SkillIcon, name: 'Behance', size: 'w-7 h-7', style: { top: '80%', left: '25%' } }, 
        { component: SkillIcon, name: 'Xd', size: 'w-7 h-7', style: { top: '78%', left: '79%' } }, 
        { component: SkillIcon, name: 'JS', size: 'w-7 h-7', style: { top: '65%', left: '85%' } }, 
        
        // Small Glyphs
        { component: OrbitingGlyph, name: 'AI', style: { top: '45%', left: '20%' } }, 
        { component: OrbitingGlyph, name: 'Code', style: { top: '45%', left: '80%' } }, 
        { component: OrbitingGlyph, name: 'Gear', style: { top: '55%', left: '90%' } }, 
        { component: OrbitingGlyph, name: 'Box', style: { top: '85%', left: '60%' } }, 
    ];
    
    // --- Generate Fanning Paths ---
    const fanningPaths = topSkills.map((skill, index) => {
        const { absX, absY } = getAbsPosition(skill.style);
        
        let cX: number, cY: number;
        
        if (absX < CORE_X) { // Left side
            cX = CORE_X - (CORE_X - absX) * 0.5;
            cY = CORE_Y - (CORE_Y - absY) * 0.5;
        } else { // Right side
            cX = CORE_X + (absX - CORE_X) * 0.5;
            cY = CORE_Y - (CORE_Y - absY) * 0.5;
        }

        cY = 300; // Adjust control point vertically for the fan-out appearance

        return (
            <path 
                key={index} 
                className="fanning-path" 
                d={`M ${CORE_X} ${CORE_Y} Q ${cX} ${cY}, ${absX} ${absY}`} 
            />
        );
    });

    return (
        // Adjust classes based on isBackground prop
        <section 
            className={`relative w-full h-full 
                      text-gray-200 overflow-hidden min-h-screen bg-gray-900`}
        >
            
       


            {/* --- Main Visualization Area --- */}
            <div 
                // Adjust height for background mode
                className={`relative w-full h-full mx-auto 
                           flex items-center justify-center`}
              
            >

                {/* SVG Container for Fanning Lines & Orbital Paths */}
                <svg className="absolute inset-0 w-full h-full pointer-events-none z-0" viewBox={`0 0 ${VIEWBOX_W} ${VIEWBOX_H}`}>
                    
                    <style>{`
                        .fanning-path {
                            stroke: #a78bfa; 
                            stroke-width: 1.5;
                            opacity: 0.2; /* Reduced opacity for background */
                            fill: none;
                        }
                        @keyframes orbit-1 {
                            from { transform: rotate(0deg); }
                            to { transform: rotate(360deg); }
                        }
                        @keyframes orbit-2 {
                            from { transform: rotate(0deg); }
                            to { transform: rotate(-360deg); }
                        }
                    `}</style>
                    
                    {/* 1. Orbital Paths */}
                    <ellipse cx={CORE_X} cy={CORE_Y} rx="380" ry="250" stroke="#c084fc" strokeWidth="1.5" fill="none" opacity={0.05} 
                                style={{ animation: 'orbit-1 40s linear infinite', transformOrigin: `${CORE_X}px ${CORE_Y}px` }}/>
                    <ellipse cx={CORE_X} cy={CORE_Y} rx="300" ry="300" stroke="#a78bfa" strokeWidth="0.8" fill="none" opacity={0.075} 
                                style={{ animation: 'orbit-2 60s linear infinite', transformOrigin: `${CORE_X}px ${CORE_Y}px` }}/>

                    {/* 2. Fanning Lines */}
                    {fanningPaths}

                </svg>

                {/* Fanning Skills - Logos. */}
                {topSkills.map((skill, i) => (
                    <SkillIcon 
                        key={`top-${i}`} 
                        name={skill.name} 
                        size="w-8 h-8 md:w-10 md:h-10" 
                        style={skill.style} 
                    />
                ))}

                {/* --- Rotating Container for Orbiting Icons --- */}
                <div 
                    className="absolute w-full h-full" 
                    style={{ 
                        animation: 'orbit-1 40s linear infinite', 
                        transformOrigin: '50% 62%' 
                    }}
                >
                    {/* Orbiting Icons */}
                    {orbitingIcons.map((item, i) => {
                         if (item.component === OrbitingGlyph) {
                            return (
                                <OrbitingGlyph 
                                    key={`orbit-${i}`} 
                                    name={item.name} 
                                    style={item.style}
                                />
                            );
                        }

                        // SkillIcon (Logo)
                        return (
                            <SkillIcon 
                                key={`orbit-${i}`} 
                                name={item.name} 
                                size={item.size}
                                style={{ ...item.style, relX: 1, relY: 1 }} 
                            />
                        );
                    })}
                </div>


                {/* Central Sigma Icon - The anchor point */}
                <div className="absolute w-32 h-32 md:w-40 md:h-40  rounded-full flex items-center justify-center z-10 
                                 border-4 border-purple-500/50 
                                 shadow-xl shadow-purple-900"
                   style={{ 
                       top: CORE_Y / VIEWBOX_H * 100 + '%', 
                       left: CORE_X / VIEWBOX_W * 100 + '%', 
                       transform: 'translate(-50%, -50%)',
                       boxShadow: '0 0 10px rgba(109, 40, 217, 0.5), 0 0 25px rgba(129, 140, 248, 0.3), inset 0 0 10px rgba(109, 40, 217, 0.2)'
                   }}>
                    <span className="text-2xl md:text-4xl font-light text-white font-serif"
                            style={{ 
                                textShadow: '0 0 5px #c084fc, 0 0 15px #a78bfa, 0 0 30px #7c3aed, 0 0 45px #6d28d9'
                            }}>
                      Skills
                    </span>
                </div>

            </div>
        </section>
    );
};

// ====================================================================
// --- ABOUTSECTION Component (Original Content with Merged Logic) ---
// ====================================================================

// Core competencies
const professionalCommitments = [
    { 
        icon: Code, 
        title: "LEADERSHIP", 
        detail: "Driving architectural vision and modern performance standards across development teams.", 
        color: "text-indigo-400", 
    },
    { 
        icon: Shield, 
        title: "RESILIENCE", 
        detail: "Implementing robust error handling and automated testing for guaranteed platform stability.", 
        color: "text-red-400", 
    },
    { 
        icon: GitBranch, 
        title: "OWNERSHIP", 
        detail: "End-to-end management of the product lifecycle, from concept to production monitoring.", 
        color: "text-yellow-400", 
    },
];


const AboutSection = () => {
    return (
        <section 
            id="about" 
            // 1. Set background and relative positioning for content/skill overlay
            className="relative py-32 md:py-40 bg-gray-900 text-white px-4 sm:px-6 lg:px-8 overflow-hidden"
        >
            
            {/* 2. BACKGROUND: SkillOrbitV2 */}
            <div className="absolute inset-0 z-0">
                <SkillOrbitV2 isBackground={true} /> 
            </div>

            {/* 3. FOREGROUND: About Content */}
            <div className="container mx-auto max-w-7xl relative z-10">
                
                {/* Header: Centered Subtitle */}
                <div className="text-center mb-20">
                    <p className="text-base font-semibold tracking-[0.3em] uppercase text-yellow-400">
                        FULL-STACK ARCHITECT
                    </p>
                </div>

                {/* Main Content Grid */}
                <div className="grid grid-cols-1 lg:grid-cols-12 gap-16 lg:gap-10 items-start">
                    
                    {/* Left Column (Narrative & CTA) */}
                    <div className="lg:col-span-7 space-y-8">
                        
                        {/* Main Headline - Added text shadow for readability over the background */}
                        <h2 className="text-5xl md:text-6xl font-extrabold tracking-tighter leading-tight text-white"
                            style={{ textShadow: '0 0 5px rgba(0,0,0,0.8), 0 0 10px rgba(0,0,0,0.5)' }}
                        >
                            Engineered for <span className="text-indigo-400">Scale.</span> Reliable.
                        </h2>
                        
                        {/* Narrative Summary - Added text shadow for readability over the background */}
                        <p className="text-xl text-gray-300 leading-relaxed font-normal max-w-xl"
                            style={{ textShadow: '0 0 3px rgba(0,0,0,0.8)' }}
                        >
                           My name is Mohammad, and my expertise lies in solving complex technical challenges.** I architect and build high-availability solutions using modern stacks like **Next.js, React, and Node.js**. My goal is to ensure your applications deliver unparalleled **Performance, Maintainability, and Security
                        </p>

                        {/* Professional CTA Button */}
                        <div className="pt-6">
                            <a href="#contact" 
                                className="inline-flex items-center justify-center text-lg font-bold uppercase 
                                        bg-indigo-600 hover:bg-indigo-700 text-white px-10 py-4 
                                        rounded-lg shadow-xl shadow-indigo-500/50 transition-all duration-300 transform hover:scale-[1.03] focus:ring-4 focus:ring-indigo-300">
                                Initiate Project Inquiry <ArrowRight className="ml-3 h-5 w-5"/>
                            </a>
                        </div>
                    </div>

                    {/* Right Column (Manifesto List) - Added backdrop blur and opacity to the card */}
                    <div className="lg:col-span-5 bg-gray-900/90 backdrop-blur-sm p-8 rounded-xl border border-gray-800 shadow-xl">
                        
                        <h3 className="text-xl font-bold text-white uppercase tracking-wider mb-6 border-b border-indigo-700 pb-3">
                            Engineering Manifesto
                        </h3>

                        <div className="space-y-6">
                            {professionalCommitments.map((item, index) => {
                                const Icon = item.icon;
                                return (
                                    <div key={index} 
                                        className="flex items-start space-x-4 group transition-all duration-200 p-3 -m-3 rounded-lg hover:bg-gray-800/50">
                                        
                                        {/* Icon */}
                                        <div className="flex-shrink-0 pt-1">
                                            <Icon className={`h-6 w-6 ${item.color}`} />
                                        </div>
                                        
                                        {/* Content */}
                                        <div>
                                            <h4 className="text-lg font-extrabold text-white uppercase tracking-wider group-hover:text-indigo-400 transition-colors">
                                                {item.title}
                                            </h4>
                                            <p className="text-sm text-gray-400 leading-relaxed pt-0.5">
                                                {item.detail}
                                            </p>
                                        </div>
                                    </div>
                                );
                            })}
                        </div>
                    </div>
                </div>
            </div>
        </section>
    );
};

export default AboutSection;